```
git checkout main
git pull origin main #to pull latest main version

git checkout -b new-feature # to create new branch
git add .
git commit -m "New feature progress"
git push -u origin new-feature
```
